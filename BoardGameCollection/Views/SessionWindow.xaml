<Window x:Class="BoardGameCollection.Views.SessionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BoardGameCollection.Views"
        mc:Ignorable="d"
        Title="Игровые сессии" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Игровые сессии" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Новая сессия -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="0,0,0,1" Padding="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Добавить новую сессию" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Дата -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Дата" FontWeight="Bold" Margin="0,0,0,5"/>
                        <DatePicker SelectedDate="{Binding SessionDate}" Height="25"/>
                    </StackPanel>

                    <!-- Количество игроков -->
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="Количество игроков" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding PlayersCount}" Height="25"/>
                    </StackPanel>

                    <!-- Оценка сессии -->
                    <StackPanel Grid.Column="2" Margin="10,0,0,0">
                        <TextBlock Text="Оценка сессии (1-10)" FontWeight="Bold" Margin="0,0,0,5"/>
                        <Slider Minimum="1" Maximum="10" Value="{Binding SessionRating}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                        <TextBlock Text="{Binding SessionRating}" HorizontalAlignment="Center" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>

                <!-- Результаты -->
                <StackPanel Margin="0,10,0,0">
                    <TextBlock Text="Результаты/Комментарии" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Results}" TextWrapping="Wrap" Height="60"/>
                </StackPanel>

                <!-- Кнопка добавления -->
                <Button Content="Добавить сессию" Command="{Binding AddSessionCommand}" HorizontalAlignment="Right" Margin="0,10,0,0" Width="150"/>
            </StackPanel>
        </Border>

        <!-- История сессий и статистика -->
        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- История сессий -->
            <Border Grid.Column="0" Padding="0,0,15,0">
                <StackPanel>
                    <TextBlock Text="История сессий" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                    <ListView ItemsSource="{Binding Sessions}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Дата" Width="120" DisplayMemberBinding="{Binding SessionDate, StringFormat={}{0:dd.MM.yyyy}}"/>
                                <GridViewColumn Header="Игроки" Width="80" DisplayMemberBinding="{Binding PlayersCount}"/>
                                <GridViewColumn Header="Оценка" Width="80" DisplayMemberBinding="{Binding SessionRating}"/>
                                <GridViewColumn Header="Результаты" Width="200" DisplayMemberBinding="{Binding Results}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </Border>

            <!-- Статистика -->
            <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1,0,0,0" Padding="15,0,0,0">
                <StackPanel>
                    <TextBlock Text="Статистика" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>

                    <TextBlock Text="{Binding Game.Title, StringFormat='Название игры: {0}'}" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding TotalSessionsCount, StringFormat='Всего сессий: {0}'}" Margin="0,5,0,5"/>
                    <TextBlock Text="{Binding AverageSessionRating, StringFormat='Средняя оценка: {0:F1}'}" Margin="0,5,0,5"/>
                    <TextBlock Text="{Binding CostPerSession, StringFormat='Стоимость вечера: {0:F2} руб.'}" Margin="0,5,0,5"/>

                    <!-- График (заглушка) -->
                    <Border Background="#EEE" Height="200" Margin="0,20,0,0" CornerRadius="5">
                        <TextBlock Text="График активности" VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0.5"/>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Индикатор ошибки -->
        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Red" 
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <!-- Кнопка закрытия -->
        <StackPanel Grid.Row="3" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Закрыть" Command="{Binding CloseCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Width="100"/>
        </StackPanel>
    </Grid>
</Window>